---
title: Inputs
slug: /inputs
---

# Inputs

Inputs contain credentials and parameters that are provided as environment variables that are consumed by selected actions during action plan execution.

### Configu

TruStacks integrates with [Configu](https://github.com/configu/configu) by generating Configu schemas for action plan inputs. [Click here](/tutorial/input) to learn more.

## Container

Container inputs provide parameters for publishing and authenticating against container registries.

| name | type | description |
| - | - | - |
| CONTAINER_REGISTRY | string | The full url of the container registry, used for authentication and iamge publishing (ie. `docker.io/my-repo/my-app`). |
| CONTAINER_REGISTRY_USERNAME | string | The authentication username for the container registry. |
| CONTAINER_REGISTRY_PASSWORD | string | The authentication password for the container registry. |

## SonarQube

SonarQube inputs provide variables for interacting with a [SonarQube](https://www.sonarsource.com/products/sonarqube/) code quality scanning service.

| name | type | description |
| - | - | - |
| SONARQUBE_TOKEN | string | The authentication token for the SonarQube instance. |

## Argo CD

Argo CD inputs provide parameters for authenticating and interacting with [Argo CD](https://argo-cd.readthedocs.io/en/stable/) service instances.

| name | type | description |
| - | - | - |
| ARGOCD_SERVER | string | the address of the argocd server. |
| ARGOCD_AUTH_TOKEN | string | the authentication token for the argocd server. |