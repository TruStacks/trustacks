---
title: Run
hide_title: true
slug: /actions/tox/run
---

import toxIcon from "../../assets/tox.png"

<img style={{height:'75px', margin: '20px 0 20px 0'}} src={toxIcon} />

# Tox - run

The run action runs the python test tasks using [tox](https://docs.pytest.org/en/7.4.x/).

### Python Version

The [python version](/configuration/python) can be manually specified via [declarative configuration](/configuration).

:::tip
This action uses the [`tox.ini`](https://tox.wiki/en/latest/config.html) in project root.
:::
